<%= form_with(model: [@movie, @review], local:true) do |r|  %>
  <%= render "shared/errors", object: @review %>
  
  <%= r.label :comment %>
  <%= r.text_area :comment, :rows => 10, style: 'width:100%; height:100%', placeholder: "What did you think?" %>
  <%
=begin%>
 <%= r.label :stars %>
  <%= r.select :stars, Review::STARS, prompt: "Select" %>  
<%
=end%>

  <%= r.label :stars %>
  <% Review::STARS.each do |star| %>
    <span class="star">
      <%= r.radio_button :stars, star %>
      <%= label_tag star %>
    </span>
  <% end %>
  
  <%= r.submit "Post Review"%>
<% end %>